#include "day5.h"

int main(){

    // /* ===== Q1 ===== */
    // struct Student s[2];
    // input_students(s,2);
    // display_students(s,2);

    // /* ===== Q2 ===== */
    // struct Complex a={2,3}, b={1,4}, c;

    // c=add(a,b);
    // printf("\nAdd: %.2f + %.2fi\n",c.real,c.imag);

    // c=subtract(a,b);
    // printf("Sub: %.2f + %.2fi\n",c.real,c.imag);

    // c=multiply(a,b);
    // printf("Mul: %.2f + %.2fi\n",c.real,c.imag);

    // /* ===== Q3 ===== */
    // float price[3]={200,400,100};
    // int qty[3]={1,2,3};
    // print_bill(price,qty,3);

    /* ===== Q4 ===== */
    play_game();

    return 0;
}